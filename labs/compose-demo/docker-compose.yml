version: "3.9"

services:
  web:
    image: nginx:1.27-alpine
    ports:
      - "8081:80"
    restart: unless-stopped
  cache:
    image: redis:7-alpine
    command: ["redis-server", "--save", "60", "1"]
    restart: unless-stopped
